/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '',
  assetPrefix: '',
  reactStrictMode: true,
  sassOptions: {
    includePaths: ['./styles'],
  },
}

module.exports = nextConfig
